import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const Settings = () => {
  return (
    <Container>
      <Box sx={{ my: 4 }}>
        <Typography variant="h4">통계</Typography>
        <Typography sx={{ mt: 2 }}>통계 페이지 준비중입니다.</Typography>
      </Box>
    </Container>
  );
};

export default Settings;
