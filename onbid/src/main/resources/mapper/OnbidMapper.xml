<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pci.onbid.mapper.OnbidMapper">

    <!-- ✅ 데이터 삽입 -->
    <insert id="insert" parameterType="com.pci.onbid.domain.OnbidItem">
        INSERT IGNORE onbid_item (
        plnm_no, cltr_nm, ldnm_adrs,
        min_bid_prc, apsl_ases_avg_amt,
        pbct_begn_dtm, pbct_cls_dtm,
        pbct_cltr_stat_nm, sido, created_at
        )
        VALUES (
        #{plnmNo}, #{cltrNm}, #{ldnmAdrs},
        #{minBidPrc}, #{apslAsesAvgAmt},
        #{pbctBegnDtm}, #{pbctClsDtm},
        #{pbctCltrStatNm}, #{sido}, NOW()
        )
    </insert>

    <!-- ✅ 전체 조회 -->
    <select id="findAll" resultType="com.pci.onbid.domain.OnbidItem">
        SELECT
        id, plnm_no AS plnmNo, cltr_nm AS cltrNm,
        ldnm_adrs AS ldnmAdrs, min_bid_prc AS minBidPrc,
        apsl_ases_avg_amt AS apslAsesAvgAmt,
        pbct_begn_dtm AS pbctBegnDtm, pbct_cls_dtm AS pbctClsDtm,
        pbct_cltr_stat_nm AS pbctCltrStatNm, sido, created_at AS regDate
        FROM onbid_item
        ORDER BY id DESC
    </select>

    <!-- ✅ 세분화 검색 (AND 기반) -->
    <select id="searchAdvanced" resultType="com.pci.onbid.domain.OnbidItem">
        SELECT
        id, plnm_no AS plnmNo, cltr_nm AS cltrNm,
        ldnm_adrs AS ldnmAdrs, min_bid_prc AS minBidPrc,
        apsl_ases_avg_amt AS apslAsesAvgAmt,
        pbct_begn_dtm AS pbctBegnDtm, pbct_cls_dtm AS pbctClsDtm,
        pbct_cltr_stat_nm AS pbctCltrStatNm, sido, created_at AS regDate
        FROM onbid_item
        WHERE 1=1
        <if test="region != null and region != ''">
            AND ldnm_adrs LIKE CONCAT('%', #{region}, '%')
        </if>
        <if test="category != null and category != ''">
            AND cltr_nm LIKE CONCAT('%', #{category}, '%')
        </if>
        <if test="status != null and status != ''">
            AND pbct_cltr_stat_nm LIKE CONCAT('%', #{status}, '%')
        </if>
        <if test="minPrice != null">
            AND CAST(min_bid_prc AS UNSIGNED) &gt;= #{minPrice}
        </if>
        <if test="maxPrice != null">
            AND CAST(min_bid_prc AS UNSIGNED) &lt;= #{maxPrice}
        </if>
        ORDER BY id DESC
    </select>

</mapper>
